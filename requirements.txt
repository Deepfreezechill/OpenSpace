# ──────────────────────────────────────────────────────
# Dependencies are managed in pyproject.toml (single source of truth).
# Install with: pip install -e ".[dev]"
#
# This file exists for tools that only read requirements.txt.
# It re-exports the core deps from pyproject.toml.
# ──────────────────────────────────────────────────────

litellm>=1.83.0,<2
python-dotenv>=1.0.0,<2
openai>=1.0.0,<3
jsonschema>=4.25.0,<5
mcp>=1.0.0,<2
anthropic>=0.71.0,<1
pillow>=11.0.0,<13
numpy>=1.24.0,<3
colorama>=0.4.6,<1
flask>=3.1.0,<4
pyautogui>=0.9.54,<1
pydantic>=2.12.0,<3
requests>=2.32.0,<3
structlog>=24.0.0,<26
prometheus-client>=0.21.0,<1
